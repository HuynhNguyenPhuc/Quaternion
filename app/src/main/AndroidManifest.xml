<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <supports-gl-texture android:name="GL_OES_compressed_ETC1_RGB8_texture" />
    <supports-gl-texture android:name="GL_OES_compressed_paletted_texture" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quaternion"
        tools:targetApi="31">
        <activity
            android:name=".ViewActivity"
            android:exported="true"
            android:theme="@style/Theme.Quaternion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Mode1Activity"></activity>
        <activity android:name=".Mode2Activity"></activity>
        <activity android:name=".Mode2ViewActivity"></activity>
        <activity android:name=".Mode3Activity"></activity>
        <activity android:name=".Mode3ViewActivity"></activity>
    </application>
</manifest>